import { Screen } from "@renderer/components/Screen.js";

export function IMScreen() {
  return (
    <Screen withLogoutButton withFeaturesButton>
      <code>inv mgmt screen</code>
    </Screen>
  );
}
