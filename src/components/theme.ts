import { extendTheme } from '@chakra-ui/react';

// TODO: Define custom theme?
let colors = {
  /* Insert custom color, theming here */
};

const customizedTheme = extendTheme({ colors });

export default customizedTheme;
